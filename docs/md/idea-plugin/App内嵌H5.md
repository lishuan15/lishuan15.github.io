#  App内嵌H5屈光档案

1.H5链接为:http://localhost:8080/wechat/?route=jdf

2.H5登陆方法名称

> ```
> h5Login({phonenumber:19919910664})
> //参数要求为json字符串格式传入
> ```

3.H5主题背景色设置

> ```
> setThemeVariables({bgColor: '#4a76de'})
> //参数要求为json字符串格式传入
> ```

